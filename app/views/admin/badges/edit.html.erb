<div class="col col-12 col-md-8">
  <h1 class="text-center">
    <%= t('.header', name: @badge.name) %>
    <%= link_to t('links.back'), admin_badge_path(@badge), class: "btn btn-secondary m-2" %>
  </h1>
  <%= render 'form', badge: @badge %>
</div>
