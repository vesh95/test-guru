<nav class="navbar navbar-expand-lg navbar-light bg-white px-5">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler nav-link" type="button" data-toggle="collapse" data-target="#navbar-actions" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  <div class="collapse navbar-collapse justify-content-center" id="navbar-actions">
    <div class="navbar-nav">
      <%= link_to t('tests.link'), tests_path,class: "nav-link" %>
      <div class='nav-item dropdown'>
        <%= render 'shared/admin/admin_actions' %>
      </div>
      <%= link_to t('feedback.link'), feedback_path, class: "nav-link" %>
    </div>
  </div>
  <% if user_signed_in? %>
      <div class='nav-item btn-group'>
        <%= link_to current_user.email, profile_path, class:"btn btn-outline-secondary" %>
        <%= link_to t('navbar.logout'), destroy_user_session_path, method: :delete, class: 'btn btn-sm btn-outline-danger nav-link' %>
      </div>
  <% end %>
</nav>
