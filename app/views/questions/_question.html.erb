<tr>
  <td><%= question.body %></td>
  <td><%= link_to '📋', question_path(question)%></td>
  <td><%= link_to '✏️', edit_question_path(question) %></td>
  <td><%= link_to '❌', question_path(question), method: :delete %></td>
</tr>
